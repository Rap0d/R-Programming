# 데이터 : sms_spam.csv
# type 컬럼 : 메일의 종류(햄/스팸), text 컬럼 : 메일의 내용
# 
# 텍스트 마이닝(tm) 패키지를 사용하여 말뭉치(Corpus)를 생성한다.
# 텍스트 → 벡터 → 말뭉치(Corpus)로 변경
# 
# tm_map() 함수를 이용하여 mapping 처리를 수행한다.
# tm_map() 함수를 이용하여 전처리를 수행해주는 함수이다.
# 
# 전처리가 끝난 말뭉치를 토큰화 시킨다.
# 이 토큰들을 이용하여 희소 matrix(행렬)를 생성한다.
# 
# 다음 데이터들에 대하여 훈련과 테스트 데이터 셋을 분리한다.(3대1)
# 원시 데이터, 말뭉치, 희소 행렬 
# 워드 클라우드 시각화를 수행해본다.