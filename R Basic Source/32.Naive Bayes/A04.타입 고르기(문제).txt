# 데이터 : personinfo.csv
# 대한 회사는 이전에 출시한 신제품에 대하여 각 고객의 선호도 정보를 저장하고 있다.
# job(직업), gender(성별), age(나이), ismarried(결혼 여부), hascar(차량 보유 유무), type(선호 타입)
# age 컬럼은 범주형 데이터로 코딩 변경을 수행하도록 한다.
# 10대(10~19), 20대(20~29), 30대(30~39), 40대(40~49) 등등
# 
# 이 데이터를 이용하여 naive bayes 알고리즘을 수행해 보세요.
# 앞의 30개의 행을 훈련용으로 사용하도록 한다.
# naiveBayes 모델을 생성한 다음, 테스트용 데이터로 검정을 하도록 한다.
# confusion matrix를 이용하여 정확도를 확인해본다.